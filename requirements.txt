# Dependências do projeto.
#
# Instalação:
#     pip install -r requirements.txt

# --- Aplicação web -------------------------------------------------------
Flask>=3.0              # microframework web e roteamento
                        # (traz o Werkzeug junto, usado no hash de senha)
python-dotenv>=1.0      # lê as credenciais do arquivo .env

# --- Inteligência artificial ---------------------------------------------
google-generativeai>=0.8

# --- Geração do PDF ------------------------------------------------------
reportlab>=4.0

# --- Testes --------------------------------------------------------------
pytest>=8.0
